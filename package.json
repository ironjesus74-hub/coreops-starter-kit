{
  "name": "coreops-starter-kit",
  "version": "1.0.0",
  "description": "CoreOps Platform — Cloudflare Workers + Static Assets deployment",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secrets:set-atlas-key": "wrangler secret put ATLAS_AI_API_KEY"
  },
  "devDependencies": {
    "wrangler": "^3.101.0"
  },
  "engines": {
    "node": ">=18"
  }
}
